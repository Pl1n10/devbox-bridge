"""Tool MCP — git.

TODO (step 8):
  Read:
    - git_status(project) -> porcelain parsato in struct
    - git_diff(project, staged=False, path=None) -> testo unified diff
    - git_log(project, limit=20) -> list[CommitInfo]
    - git_branch_current(project) -> str
  Write:
    - git_create_branch(project, name)
    - git_commit(project, message, paths)  # mai -a, paths obbligatori
    - git_push(project, remote='origin')   # solo se allow_push=true

  NON IMPLEMENTARE: reset --hard, push --force, clean, branch -D.
"""

# placeholder — implementazione nello step 8
