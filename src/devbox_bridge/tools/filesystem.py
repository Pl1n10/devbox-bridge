"""Tool MCP — filesystem.

TODO (step 6):
  - list_projects() -> list[ProjectInfo]
  - read_file(project: str, path: str) -> {content, size, mtime, is_text}
  - list_directory(project: str, path: str) -> list[Entry]  # skip nm/.git/etc
  - search_files(project: str, pattern: str, glob: str = "*") -> JSON da rg
  - write_file(project: str, path: str, content: str, create: bool = False)  # write
  - apply_patch(project: str, path: str, old: str, new: str)  # write, str-replace
"""

# placeholder — implementazione nello step 6
