"""Fixture pytest condivise.

TODO (popolare ad ogni step):
  - tmp_project_root: tmp_path con qualche file/dir per test fs/git
  - sample_config: AppConfig in-memory con un progetto temporaneo
  - tmp_token_file: file con sha256 di un token noto
"""

import pytest


@pytest.fixture
def placeholder() -> None:
    """Fixture placeholder — sostituire con quelle reali via via."""
    return None
